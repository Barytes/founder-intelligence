services:
  rsshub:
    image: diygod/rsshub
    container_name: rsshub
    restart: unless-stopped
    ports:
      - "1200:1200"
    env_file:
      - ../.env
    environment:
      NODE_ENV: production
      CACHE_TYPE: memory
